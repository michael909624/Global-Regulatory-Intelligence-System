今日日期：{today}

任务：在【{dim_name}】维度下，基于已有 {static_count} 个静态议题，补充**最多 {max_extra} 个不重叠**的新兴议题。

产品覆盖范围（议题图需覆盖整机与零部件）：
{category_block}

已有静态议题（共 {static_count} 个，已覆盖该维度的核心监管面，你**不需要**重新枚举它们）：
{static_topics_block}

═══════════════════════════════════════════════════════
关键要求（违反任一条 → 该议题会被丢弃）
═══════════════════════════════════════════════════════

1. **不重叠**
   你提议的新议题不能与上述 {static_count} 个静态议题中**任何一条**的 scope_hint 涵盖范围重叠。
   重叠的判定标准:同一份法规会被两个议题同时命中 → 视为重叠 → 丢弃。

2. **必须新兴**
   仅当近 12 个月内出现了静态全集没覆盖到的**新监管动向**时才补充。例:
     - 新法域立法（某国/某州首次推出）
     - 跨界主题（如 AI + 物理产品的新合规框架）
     - 静态全集成型时还不存在的新规

3. **宁可少不可凑**
   - 如果你判断没有值得补充的新议题,**直接返回空数组 []**
   - 不重要的议题只会浪费后续 fetch 阶段的搜索调用,反而拉低信噪比
   - 不要为了"凑满 {max_extra} 个"而拆碎已有议题

4. **每个新议题的 scope_hint 必须末尾附理由**
   格式:scope_hint 末尾用括号注明 "(静态议题 X / Y 没覆盖到，因为 Z)"
   方便后续审核是否值得长期保留。

═══════════════════════════════════════════════════════

输出严格 JSON 数组（可以为空 []，最多 {max_extra} 个对象）：

[
  {{
    "id":         "{dim_lower}-... 短稳定 ID,小写连字符",
    "name":       "议题中文名（≤20 字）",
    "scope_hint": "覆盖内容、典型法域、典型框架（末尾括注新增理由）"
  }}
]

不含解释文字、不含 markdown 代码块、不含 [1][2] 等引用标记。
没有真新增就直接返回 []，不要勉强填充。
